#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""
生成适配「本机路径」的两份 WorkBuddy 自动化提示词（v9）。

用法:
    python print_automation_prompts.py

把输出的每一段复制粘贴到新电脑 WorkBuddy 的「自动化」里即可，
路径已自动填好，不需要手动替换。

配套创建自动化时的设置:
  自动化1  名称: A股盘中盯盘（15min量价异动 v9）
           周期: 每工作日 13:30
  自动化2  名称: A股持仓跟踪（T+1~T+5 出场判定）
           周期: 每工作日 16:00
           （不要用 15:30：实测 15:31 运行时日K 尚未回填，当天出场判定会全部漏掉）
"""
import sys
from pathlib import Path

HERE = Path(__file__).parent.resolve()
PY = Path(sys.executable)

D = str(HERE)
TOP5 = str(HERE / 'results_live' / 'top5_<日期>.csv')
HTML = str(HERE / 'results_live' / 'live_<日期>.html')
POS = str(HERE / 'results_live' / 'positions.json')

P1 = f"""执行 A 股盘中盯盘扫描，捕捉量价异动个股，直接给出当日最值得关注的个股（最多 3 只）。

操作方式（在目录 {D} 下执行）：
{PY} live_scout.py --pool=400 --workers=12

脚本逻辑（v9，依据 2024-09~2026-08 共 24 个月、11469 条信号回测定参）：
1. 全市场快照预筛候选池（成交额前 400，剔除 ST/北交/极端涨跌）；
2. 计算近 5 个交易日累计跌幅，只保留下跌的票（A 级 ≤ -3%，B 级 ≤ -1%；每天只算一次并缓存）；
3. 拉 15 分钟 K 线，找当根涨幅 0~2%、成交量 ≥ 前一根 2 倍的脉冲 bar，排除 11:30、13:15、15:00 三根；
4. 同期放量 = 今日截至该 bar 的累计量 ÷ 昨日截至同一 bar 的累计量，**要求 ≥ 1.5×**（v9 核心改动，原为 0.9）；
5. 同一只票当天多根 bar 触发时，只保留同期放量最强的那根；
6. 按「同期放量倍数」降序排序，最多取 3 只；
7. 强度分级：★★★ ≥2.3×、★★ ≥1.8×、★ ≥1.5×；
8. 追高保护：用实时价计算「现价 vs 信号价」偏离，超过 +3% 标「慎追」；
9. 计算平静度（前 3 根量 最大/最小）与爆发（信号根量/前 3 根均量），仅标注不过滤；
10. 大盘择时（上证 > MA20）默认关闭，只在输出里提示大盘状态。

v9 门槛调整的依据（回测按今/昨量比分档，已扣 0.25% 手续费印花税，呈严格单调）：
    0.9~1.2  PF 1.33  净均 +0.61%   ← 旧门槛 0.9 把这档全放进来，是主要拖累
    1.2~1.6  PF 1.64  净均 +1.03%
    1.6~2.2  PF 1.90  净均 +1.40%
    >2.2     PF 2.63  净均 +2.17%

输出文件（脚本已写好，直接读，不要自己重算）：
- {TOP5}　← 当日入选个股（最多 3 只），直接用这个
- {HTML}　← 完整报告，TOP 清单在最顶部

结果处理（严格执行）：
- 有信号时：读 top5_<日期>.csv，输出表格（列：#、名称、代码、触发 bar、信号价、近 5 日跌幅、瞬时量比、同期放量、强度、现价偏离）。表格后说明操作：信号根收盘价买入、每只 1 万元、T+1 起 5 个交易日内日 K 收盘 ≥ 买入价×1.05 卖出、第 5 日收盘强平、**不加止损**。再用 present_files 打开 live_<日期>.html。
- **宁缺毋滥**：不足 3 只就是当日没有好机会，明确告诉用户「今日只选出 N 只，不建议为凑数买入」，绝不要从全量信号里补低质量的凑满。
- **追高提醒**：若某只「现价偏离」标了「慎追」，必须单独点名——该股已较信号价上涨 3% 以上，此时下单等同追高，建议放弃或等回落，不要因为它排在前面就推荐。
- 提醒时效：表中信号价是信号根收盘价，若当前时间距触发 bar 已超过 30 分钟，提示下单前按市价核对。
- 若无信号：一句话说明原因即可（今日无个股达到同期放量 1.5× 门槛，属正常），不要长篇输出，不要调用 present_files。
- 若脚本报错：读输出定位原因（常见为数据源限流），简要说明，重试不超过 2 次。"""

P2 = f"""执行 A 股信号股持仓跟踪，判定出场并汇报。

操作方式（在目录 {D} 下执行，按顺序）：

1. 先跑一次当日扫描以获得完整的全天信号（收盘后 OHLC 已全部回填）：
{PY} live_scout.py --pool=400 --workers=12

2. 用今天的日期建立新持仓（每天最多 3 只，按同期放量倍数降序）：
{PY} track_positions.py --init=YYYY-MM-DD --max-positions=3
（把 YYYY-MM-DD 替换为当天实际日期；若当日无信号文件或已持仓，脚本会自动跳过，属正常）

3. 更新已有持仓并判定出场：
{PY} track_positions.py

规则说明（v9，脚本已内置，依据 24 个月 / 11469 条信号回测）：
- 买入价 = 信号根 15min 收盘价，每只约 1 万元（按 100 股整手取整）
- T+1 起 5 个交易日内，日 K 收盘价 >= 买入价 × 1.05 即止盈卖出
  （由 1.01 上调到 1.05。回测中止盈 +1% 时 PF 1.13、平均单笔 +0.15%，
   放宽到 +5% 后 PF 1.35、平均单笔 +0.71% —— +1% 出场过早，把利润让出去了）
- 第 5 个交易日收盘仍未止盈则强平
- 不加止损（回测证实加止损会把先挖坑再反弹的票砍掉，PF 反而从 1.13 降到 0.87）
- 状态保存在 results_live/positions.json

**强烈建议提醒用户挂条件单**：止盈判定用的是「日 K 收盘价」，盘中冲高回落是吃不到的。
例如 2026-09-09 英唐智控盘中最高较买入价 +3.9%、收盘反而 -3%，这类情况收盘判定无法捕捉。
解决办法是买入当天就在券商 APP 挂「+5% 止盈条件单」，盘中触发自动卖出，不依赖盯盘。
每次新建持仓后，请在回复中提醒用户为这几只挂上条件单。

结果处理：
- 用 present_files 打开 {POS} 供查看（仅当有持仓变动时）。
- 在回复中列出：本次新出场的股票（名称、代码、止盈/强平、T+N、买入价→卖出价、收益率、盈亏金额），以及仍在持仓的股票（名称、代码、买入价、已持有天数、相对买入价的最新涨跌）。
- 若有累计已平仓记录，务必给出汇总：笔数、胜率、合计盈亏（元）、盈亏因子 PF。
- 若当日无信号、无持仓、无出场，一句话说明即可，不要长篇输出，不要调用 present_files。
- **日K回填兜底**：若发现取不到当日日 K（脚本零出场且明显有持仓），说明数据源尚未回填，
  等待 10 分钟后重试一次；仍不行就直接说明情况，下次执行会自动补记，不要手改 positions.json。
- 若脚本报错：读取输出定位原因（常见为数据源限流），简要说明，重试不超过 2 次。"""


def main():
    bar = '=' * 78
    print(bar)
    print('  自动化 1 / 2：A股盘中盯盘（15min量价异动 v9）')
    print('  周期：每工作日 13:30　状态：ACTIVE')
    print('  把下面虚线之间的内容整段复制')
    print(bar)
    print()
    print('-' * 78)
    print(P1)
    print('-' * 78)

    print()
    print()
    print(bar)
    print('  自动化 2 / 2：A股持仓跟踪（T+1~T+5 出场判定）')
    print('  周期：每工作日 16:00　状态：ACTIVE')
    print('  （勿用 15:30：日K 未回填会导致当天出场判定全部漏掉）')
    print('  把下面虚线之间的内容整段复制')
    print(bar)
    print()
    print('-' * 78)
    print(P2)
    print('-' * 78)
    print()
    print('提示：新建自动化时记得把「工作目录」设为：')
    print(f'  {D}')


if __name__ == '__main__':
    main()
